<font color="#13B7FD" size="15"><b>Organization & Architecture</b></font><br>
<br>
<br><font color="#cccccc" size="11"><b>NumberBaseConversionDriver.java</b></font>
<br>
/*<br>
file  :NumberBaseConversionDriver<br>
date  : 2/21/05<br>
author: Hardik Bhatt<br>
description: This is the main or the driver where the method created in the other class work for<br>
in this method we ask the user firs the b conversion. where the user gives us what base he wants to<br>
Converts and then the number and the method uses convertB method to base an answer in base 10 to the user.<br>
Then a second question is asked where the user gives up a base 10 number and we convert that number to a<br>
binary or base 2 number.<br>
*/<br>
import java.io.*;<br>
import java.util.*;<br>
import java.lang.*;<br>
<br>
public class NumberBaseConversionDriver<br>
{<br>
    public static void main (String[] args) throws IOException<br>
    {<br>
	//NumberBaseConversion constructor<br>
	NumberBaseConversion Convert = new NumberBaseConversion ();<br>
<br>
	//Prompts the user to give us his base he wants to convert<br>
	System.out.print ("Please specify your base: ");<br>
	BufferedReader Binput = new BufferedReader (new InputStreamReader (System.in));<br>
	String Bline;<br>
	Bline = Binput.readLine ();<br>
<br>
	//promps the user to give us the number in the given base he wants to convert <br>
	//to a base 10 number<br>
	System.out.print ("Please enter the base numbers you want to convert: ");<br>
	BufferedReader input1 = new BufferedReader (new InputStreamReader (System.in));<br>
	String line1;<br>
	line1 = input1.readLine ();<br>
	Convert.ConvertB (line1, Bline);<br>
<br>
	//promps the user to specify a base 10 number to be converted to base 2.<br>
	System.out.println ();<br>
	System.out.println ("Number Conversion");<br>
	System.out.print ("Enter a non-negative integer: ");<br>
	BufferedReader input2 = new BufferedReader (new InputStreamReader (System.in));<br>
	String line2;<br>
	line2 = input2.readLine ();<br>
	int Num = Integer.parseInt (line2);<br>
	Convert.Convert10 (Num);<br>
<br>
    }<br>
}<br>
<br>
<br><font color="#cccccc" size="11"><b>NumberBaseConversion.java</b></font>
<br>
/*<br>
file  :NumberBaseConversion<br>
date  : 2/21/05<br>
author: Hardik Bhatt<br>
description: This Program Carries 2 methods. The forst method is mearly<br>
a default constructor which help with the interaction between two classes.<br>
The secon method is the Convert10 with converts a given base 10 number into a base 2 number.<br>
The last methos is the ConvertB Which converts a given base b number to a base 10 number.<br>
*/<br>
import java.util.*;<br>
import java.lang.*;<br>
public class NumberBaseConversion<br>
{<br>
    public NumberBaseConversion ()  //Default constructor<br>
    {<br>
	int num = 0;<br>
	int base = 0;<br>
    }<br>
<br>

    //Method that converts a given base 10 numbe into a base 2 number or<br>
    //a binary number.<br>
    public static void Convert10 (int num)<br>
    {<br>
	String binary = ""; //string that holds the binary number in order<br>
	int rem; //holds the number (1 or 0) to be added into the binary string<br>
<br>
	while (num != 0)<br>
	{<br>
	    rem = num % 2; //gets the remainder<br>
	    num = num / 2; //devides the given number by 2 continuesly untill the number is 0<br>
	    binary = rem + binary; //holds and then adds the number<br>
	}<br>
	System.out.println ("The Binary Number is " + binary);<br>
    }<br>
<br>

    //This method converst a given base B number given by the user and converts<br>
    //the number into a base 10 number<br>
    public static void ConvertB (String BaseNum, String TheBase)<br>
    {<br>
	int numHolder; //holds an individual number<br>
	int Base10Num = 0; //holds the final answer in base 10<br>
	int baseHolder = Integer.parseInt (TheBase); //holds the base B number given by the user<br>
	int PowerNumber = 0; //a type of counter<br>
	int basenumber = 0; //hold the length of the given number<br>
	for (int i = 0 ; i %3C BaseNum.length () ; i++)<br>
	{<br>
	    numHolder = Character.getNumericValue (BaseNum.charAt (i));<br>
	    //baseHolder = FindBase (BaseNum);<br>
	    basenumber = BaseNum.length () - (i + 1);<br>
	    for (int j = 0 ; j %3C basenumber ; j++)<br>
	    {<br>
		if (basenumber == 1)<br>
		    PowerNumber = baseHolder;<br>
		else<br>
		    PowerNumber += baseHolder * baseHolder;<br>
	    } //End for loop<br>
	    if (basenumber == 0)<br>
		PowerNumber = 1;<br>
	    Base10Num += (numHolder * PowerNumber);<br>
	    PowerNumber = 0;<br>
	} //End forloop<br>
	System.out.println ("The Base is " + baseHolder);<br>
	System.out.println ("The Base 10 Number is " + Base10Num);<br>
    } //end ConvertB<br>
<br>

    /*<br>

	public static int FindBase (String BaseNum)<br>
	{<br>
	    StringTokenizer tokenizer = new StringTokenizer (BaseNum);<br>
	    String str = tokenizer.nextToken ();<br>
	    char Base = ' ';<br>
	    int Counter = BaseNum.length ()-1;<br>
<br>
	    for (int i = 0 ; i %3C= Counter ; i++)<br>
	    {<br>
		if (str.charAt (i) %3C str.charAt (Counter))<br>
		{<br>
		    Base = str.charAt (Counter);<br>
		}<br>
		else if (str.charAt (i) > str.charAt (Counter))<br>
		{<br>
		    Base = str.charAt (i);<br>
		}<br>
		Counter--;<br>
	    }<br>
	    num = Character.getNumericValue (Base) + 1;<br>
	    return num;<br>
	}<br>
	*/<br>
}<br>
<br>
<br>Output:<br>
Please specify your base: 5<br>
Please enter the base numbers you want to convert: 43<br>
The Base is 5<br>
The Base 10 Number is 23<br>
<br>
Number Conversion<br>
Enter a non-negative integer: 23<br>
The Binary Number is 10111<br>
<br>